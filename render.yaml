services:
  - type: web
    name: aazim-portfolio
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: out
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
